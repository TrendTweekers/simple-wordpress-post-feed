[1mdiff --git a/server/lib/shopify/session.js b/server/lib/shopify/session.js[m
[1mindex fa07b57..108517c 100644[m
[1m--- a/server/lib/shopify/session.js[m
[1m+++ b/server/lib/shopify/session.js[m
[36m@@ -2,29 +2,36 @@[m [mconst { shopifyApi, sessionStorage } = require("./shopify");[m
 [m
 /**[m
  * Safely get offline session ID, handling both newer and older Shopify API versions[m
[32m+[m[32m * NEVER throws TypeError - always returns a valid session ID string[m
  * @param {string} shop - Shop domain (e.g., "example.myshopify.com")[m
[31m- * @param {object} shopifyApiInstance - Shopify API instance[m
[31m- * @returns {string} - Offline session ID[m
[32m+[m[32m * @param {object} shopifyApiInstance - Shopify API instance (from server/lib/shopify/shopify.js)[m
[32m+[m[32m * @returns {string} - Offline session ID (format: "offline_{shop}")[m
  */[m
 function getOfflineIdSafe(shop, shopifyApiInstance) {[m
[31m-  // Newer libs: shopifyApi.session.getOfflineId(shop)[m
[31m-  if (shopifyApiInstance?.session?.getOfflineId) {[m
[31m-    return shopifyApiInstance.session.getOfflineId(shop);[m
[32m+[m[32m  if (!shop) {[m
[32m+[m[32m    console.warn(`[SESSION] getOfflineIdSafe called without shop, using fallback`);[m
[32m+[m[32m    return `offline_unknown`;[m
   }[m
   [m
[31m-  // Older libs / custom: offline_${shop}[m
[31m-  // Also fallback if Utils.session.getOfflineId exists[m
[32m+[m[32m  // Try Shopify.Utils.session.getOfflineId(shop) - this is the standard way[m
   if (shopifyApiInstance?.Utils?.session?.getOfflineId) {[m
     try {[m
[31m-      return shopifyApiInstance.Utils.session.getOfflineId(shop);[m
[32m+[m[32m      const offlineId = shopifyApiInstance.Utils.session.getOfflineId(shop);[m
[32m+[m[32m      if (offlineId && typeof offlineId === 'string') {[m
[32m+[m[32m        return offlineId;[m
[32m+[m[32m      }[m
[32m+[m[32m      console.warn(`[SESSION] getOfflineId returned invalid value: ${offlineId}, using fallback`);[m
     } catch (err) {[m
[31m-      console.warn(`[SESSION] getOfflineId failed, using fallback:`, err.message);[m
[31m-      return `offline_${shop}`;[m
[32m+[m[32m      console.warn(`[SESSION] getOfflineId threw error, using fallback:`, err.message);[m
[32m+[m[32m      // Fall through to fallback[m
     }[m
   }[m
   [m
[31m-  // Final fallback[m
[31m-  return `offline_${shop}`;[m
[32m+[m[32m  // Fallback: construct offline ID manually (format: "offline_{shop}")[m
[32m+[m[32m  // This is the standard format used by Shopify API[m
[32m+[m[32m  const fallbackId = `offline_${shop}`;[m
[32m+[m[32m  console.log(`[SESSION] Using fallback offline ID for ${shop}: ${fallbackId}`);[m
[32m+[m[32m  return fallbackId;[m
 }[m
 [m
 /**[m
[36m@@ -35,13 +42,38 @@[m [mfunction getOfflineIdSafe(shop, shopifyApiInstance) {[m
  */[m
 async function loadOfflineSession(shop) {[m
   if (!shop) {[m
[31m-    throw new Error("loadOfflineSession called without shop");[m
[32m+[m[32m    const err = new Error("loadOfflineSession called without shop");[m
[32m+[m[32m    err.status = 401;[m
[32m+[m[32m    throw err;[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  // Get offline ID safely (never throws TypeError)[m
[32m+[m[32m  let offlineId;[m
[32m+[m[32m  try {[m
[32m+[m[32m    offlineId = getOfflineIdSafe(shop, shopifyApi);[m
[32m+[m[32m  } catch (err) {[m
[32m+[m[32m    // This should never happen, but if it does, use fallback[m
[32m+[m[32m    console.error(`[SESSION] Unexpected error in getOfflineIdSafe:`, err);[m
[32m+[m[32m    offlineId = `offline_${shop}`;[m
   }[m
 [m
[31m-  const offlineId = getOfflineIdSafe(shop, shopifyApi);[m
   console.log(`[SESSION] Loading offline session for ${shop} (id=${offlineId})`);[m
 [m
[31m-  const session = await sessionStorage.loadSession(offlineId);[m
[32m+[m[32m  // Load session from storage (wrap in try-catch to prevent TypeError)[m
[32m+[m[32m  let session;[m
[32m+[m[32m  try {[m
[32m+[m[32m    if (!sessionStorage || typeof sessionStorage.loadSession !== 'function') {[m
[32m+[m[32m      throw new Error('sessionStorage.loadSession is not available');[m
[32m+[m[32m    }[m
[32m+[m[32m    session = await sessionStorage.loadSession(offlineId);[m
[32m+[m[32m  } catch (err) {[m
[32m+[m[32m    // If loadSession throws (e.g., TypeError), treat as missing session[m
[32m+[m[32m    console.error(`[SESSION] Error loading session for ${shop} (id=${offlineId}):`, err.message || err);[m
[32m+[m[32m    const loadErr = new Error(`Failed to load offline session for ${shop} (id=${offlineId}): ${err.message || err}`);[m
[32m+[m[32m    loadErr.status = 401;[m
[32m+[m[32m    loadErr.originalError = err;[m
[32m+[m[32m    throw loadErr;[m
[32m+[m[32m  }[m
 [m
   if (!session) {[m
     const err = new Error(`Offline session missing for ${shop} (id=${offlineId})`);[m
